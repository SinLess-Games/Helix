apply plugin: "war"

bootWar {
    mainClass = "com.sinlessgamesllc.HelixbotApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
